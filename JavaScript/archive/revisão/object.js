//Object
const person ={
    name: 'Marcus',
    age: 25,
    weight: 85,
    isPirelli: true,
}

//show just the name
console.log(person.name)

//Interpolation
console.log(`${person.name} is ${person.age} old.`)